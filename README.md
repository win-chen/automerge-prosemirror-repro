Reproduction for: "ProseMirror doc doesn't match AutoMerge spans."

`npm run dev`

Steps:
- Click "Create doc"
- In the editor view where "hello" is shown, press enter
- Click "Load doc" - this loads the current doc from indexeddb
- In the editor view, place cursor on the empty new line and press enter again - this logs the warning in the console "ProseMirror doc doesn't match AutoMerge spans."
